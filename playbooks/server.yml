---

- name: Configure dedicated Server
  hosts: server
  roles:
    - network
    - dns
    - role: openvpn_server
      openvpn_server: "{{ server_openvpn_servers['cloud.thomaskrille.de'] }}"
    - virtualbox
    - vagrant
