---

- name: deploy dotfiles
  git: repo=git://github.com/tkrille/dotfiles dest=~root/.dotfiles accept_hostkey=true

- name: link dotfiles
  file: src=/root/.dotfiles/.{{ item }} dest=/root/.{{ item }} state=link force=yes
  with_items:
    - zshrc
    - vimrc
    - curlrc
    - gitconfig
